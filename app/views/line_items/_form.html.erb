<%= form_for(line_item) do |f| %>
  <% if line_item.errors.any? %>
    <div id="error_explanation" class="w3-panel">
      <h2><%= pluralize(line_item.errors.count, "error") %> prohibited this line_item from being saved:</h2>

      <ul>
      <% line_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="w3-panel">
    <%= f.label :product_id , class:"montserrat lh-copy" %>
    <%= f.text_field :product_id , class:"w3-input w3-border space-mono red" %>
  </div>

  <div class="w3-panel">
    <%= f.label :cart_id , class:"montserrat lh-copy" %>
    <%= f.text_field :cart_id , class:"w3-input w3-border" %>
  </div>

  <div class="actions">
    <%= f.submit :Submit, class:"space-mono w3-mobile w3-button w3-red w3-border w3-border-red w3-hover-white w3-round-large" %>
  </div>
<% end %>
